server:
  port: 8866
management:
  endpoints:
    web:
      cors:
        exposed-headers: "*"
  endpoint:
    health:
      show-details: always
    shutdown:
      enabled: true
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/meatball?useUnicode=true&characterEncoding=utf-8&serverTimezone=Hongkong&useSSL=false
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

